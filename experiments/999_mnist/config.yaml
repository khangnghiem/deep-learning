experiment:
  name: 999_mnist
  description: mnist experiment
data:
  dataset: mnist
  train_split: 0.8
  val_split: 0.1
  test_split: 0.1
  batch_size: 64
  num_workers: 4
  silver: null
  gold:
    transform: gold.classification_gold
  sources:
  - name: mnist
model:
  architecture: resnet18
  pretrained: true
  num_classes: 10
training:
  epochs: 15
  learning_rate: 0.001
  optimizer: adam
  scheduler: cosine
  early_stopping_patience: 10
  seed: 42
mlflow:
  experiment_name: mnist
  run_name: null
paths:
  data_dir: null
  output_dir: null
